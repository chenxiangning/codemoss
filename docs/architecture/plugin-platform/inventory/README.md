# Plugin Ownership Inventory

> Wave 0A 产物。事实源是 [`ownership.json`](ownership.json)，schema 见 [`ownership.schema.json`](ownership.schema.json)。

- 当前工作树仍是完整单体。本目录只标归属，不授权删除产品代码。
- `retired-unreferenced` 目前为空。
- 重新扫描：`npm run inventory:plugin-ownership`
- 校验：`npm run check:core-shell:boundary`
