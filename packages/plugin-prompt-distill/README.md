# plugin-prompt-distill

过渡仓。`pluginId`：`com.mossx.prompt-distill`。

- 产品实现仍在：`src/features/prompt-distill`
- 产品导入走：`@mossx/plugin-prompt-distill/runtime` 与 `/ui`
- 没有：`dist/`、签名、SBOM
- 不进 Host / boot / 远程 Marketplace
- 不进入 Host 只读插排，除非后续单独声明
