# plugin-skills

过渡仓。`pluginId`：`com.mossx.skills`。

- 产品实现仍在：`src/features/skills` 与 `src/features/curated-skills`
- 产品导入走：`@mossx/plugin-skills/runtime` 与 `/ui`
- 没有：`dist/`、签名、SBOM
- 不进 Host / boot / 远程 Marketplace
- 不进入 Host 只读插排，除非后续单独声明
