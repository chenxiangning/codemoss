# plugin-intent-canvas

过渡仓。`pluginId`：`com.mossx.intent-canvas`。

- 产品实现仍在：`src/features/intent-canvas`
- 产品导入走：`@mossx/plugin-intent-canvas/runtime` 与 `/ui`
- 没有：`dist/`、签名、SBOM
- 不进 Host / boot / 远程 Marketplace
