# plugin-kanban

过渡仓。`pluginId`：`com.mossx.kanban`。

本目录不是正式 Marketplace 制品，也不是独立 Git 仓库。

- 有：`.mossx-plugin/plugin.json`
- 产品实现仍在：`src/features/kanban`
- 没有：`dist/`、签名、SBOM
- 不进 Host / boot / 远程 Marketplace
