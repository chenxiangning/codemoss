# Tasks

- [x] 1.1 落盘 proposal / design / spec delta
- [x] 1.2 `openspec validate engine-claude-process-entry-result --strict --no-interactive`
- [x] 1.3 制品根监督本机 CLI，读到 `result` 再 wait 退出码
- [x] 1.4 缺 CLI 跳过；默认仍 `cmd.spawn()`，boot 仍 missing
