# Tasks

- [x] 1.1 落盘 proposal / design / spec delta
- [x] 1.2 `openspec validate project-map-plugin-dual-run-isolated --strict --no-interactive`
- [x] 1.3 Adapter 增加 IsolatedProjectMap owner + 注入根 `isolated` / `isolated_product`
- [x] 1.4 persist 方法 Isolated 走 `ProjectMapNamespace`；scan 走 temp 导入；embed compute 仍 Core
- [x] 1.5 产品 persist 命令 flag-on 走 `isolated_product`
- [x] 1.6 测试：隔离闭环不碰产品目录；flag 默认关；无 `activate_plugin` / `dispatch_command`
